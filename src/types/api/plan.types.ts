/*
public enum SubscriptionType
{
    FREE,
    BASIC,
    PREMIUM,
    ENTERPRISE,
    INTERNAL
}
*/

export enum SubscriptionType {
    FREE = 1,
    BASIC = 2,
    PREMIUM = 3,
    ENTERPRISE = 4,
    INTERNAL = 5
}

export enum PlanStatusType {
    ACTIVE = 1,
    TRIAL = 2,
    EXPIRED = 3,
    SUSPENDED = 4,
    CANCELLED = 5
}